# Phase 5 AJAX Verification

Nonce, authentication, operation, positive-ID, criteria-array, frequency, explicit boolean, valid mutation, and user/action-scoped rate-limit behavior were executed. Accepted booleans: booleans, 0/1 integers, and `"false"`/`"true"` or `"0"`/`"1"`; other values are rejected.
